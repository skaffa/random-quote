make api endpoint where the user can read about how things are generated (including parts of code)



allow users to vote for quotes



use arrows keys to scroll through images
use enter key to go to next image
use backspace key to go to previous image
use space key to save/share image (raw image, not a url to it)

watermark:
    different font
    more padding
    different color
    different size






<!-- Vote a Quote, get a $Dote -->

<!-- 
vote_a_quote = vote
show_the_quotes = gallery
-->

<!-- 
uid based on ip
-->